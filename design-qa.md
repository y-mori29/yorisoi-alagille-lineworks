# デザインQA: アラジール ホームUI再構築 2026-06-29

対象:

- `backend/public/index.html`
- `backend/public/css/alagille-family.css`

参照画像:

- `images/alagille-home-designs-20260627/screens/01-integrated-home-final-candidate.png`
- `images/alagille-final-ui-pack-20260628/output/01-final-home-ui-spec-sheet.png`
- `images/alagille-final-ui-pack-20260628/output/16-final-design-system-component-sheet.png`

## 今回の修正方針

旧3ボタンLINE版や既存MVPの画面を流用しているように見える導線を、ホームから切り離した。

今回のホームは、UI生成画像からUI Translation Kitで翻訳した「家族ノートの入口」として扱う。  
各ボタンは旧 `/simple/...` 画面へ遷移させず、ホーム内の確認シートで「次に作る画面」を説明する状態にした。

## 静的確認

- `index.html` 内の旧ページ遷移リンクを検索。
- `/simple/`、`symptom-log`、`lab-tracker`、`medications` へのホーム内リンクは検出なし。
- 主要UI要素はHTML/CSSで再構築。
- 画面全体画像は背景やUI部品として使用していない。
- 下部ナビは旧設定画面へのリンクではなく、ホーム内シートを開くボタンに変更。
- HTML内インラインスクリプト構文チェックOK。
- `design/asset-manifest.json` はJSONとして読み込みOK。
- 静的確認URL `http://127.0.0.1:8082/` はHTTP 200。
- `css/alagille-family.css` と新規/差し替え主要画像5点はHTTP 200。

## 2026-06-29 森さん実ブラウザ確認での指摘

森さんの実ブラウザスクリーンショットで、次の問題を確認。

- ヘッダーの `よりそい アラジール` が患者ピルと重なり、ロゴとして成立していない。
- ロゴをHTML文字で出しており、仕様書で意図していた「デザイン画像としてのロゴ」になっていない。
- 患者切り替えピルがロゴへ被り、画像も見づらい。
- 家族共有バナーがただ表示されているだけで、確認できるボタンになっていない。
- 水彩素材がUI部品ではなく添え物になっている。
- 6つの主要アクションがMaterial Symbols中心で、正本画像のイラスト感を再現できていない。

## 2026-06-29 追加修正

- 正本ホーム画像から `wordmark-alagille-v1.png` を切り出し、ヘッダーのロゴ画像として使用。
- 正本ホーム画像から `patient-avatar-harukun-v1.png` と `family-share-scene-v1.png` を切り出し。
- アセットシートから6つのタイル用水彩素材を切り出し。
  - `tile-mic-v1.png`
  - `tile-notebook-pencil-v1.png`
  - `tile-test-tubes-v1.png`
  - `tile-medicine-bottle-v1.png`
  - `tile-checklist-v1.png`
  - `tile-question-bubble-v1.png`
- 家族共有バナーを `button` 化し、共有確認シートを開けるように変更。
- ホームタイルの主役を `.tile-art` の水彩画像へ変更。
- `asset-manifest.json` に新規切り出し素材を登録。

## 2026-06-30 森さん実ブラウザ確認での追加指摘

森さんの実ブラウザスクリーンショットで、前回修正後も次の問題が残っていた。

- 患者切り替えがロゴ横に置かれていて、何の操作か分かりづらい。
- `patient-avatar-harukun-v1.png` に文字と罫線が残っており、アバターとして不自然。
- `日々の様子`、`お薬メモ`、`先生に見せる` の水彩素材に切り取りミスや余分な紙端が残っている。
- 素材が「添えてあるだけ」に見える箇所があり、UI部品として翻訳しきれていない。

## 2026-06-30 追加修正

- ヘッダーを `ワードマーク + メニュー` の2要素に戻し、患者切り替えとの競合を解消。
- 患者切り替えを `.patient-selector-card` としてヘッダー直下へ移動。
- `patient-avatar-harukun-v2.png` を新規作成。正本モチーフシートの単独子どもイラストから、文字や罫線を含まない透明PNGとして切り出した。
- `日々の様子` は `tile-daily-camera-v2.png` を新規作成し、写真・動画の入口として分かりやすい素材に変更。
- `お薬メモ` は `trimmed/07-medicine-bottle.png` に差し替え。
- `先生に見せる` は `trimmed/03-medical-document.png` に差し替え。
- `.tile-art` の枠を少し広げ、画像が切れずに全体表示されるように調整。
- `index.html` から、問題のあった `patient-avatar-harukun-v1.png`、`tile-notebook-pencil-v1.png`、`tile-medicine-bottle-v1.png`、`tile-checklist-v1.png` 参照が消えていることを確認。

## 2026-06-30 静的確認

- HTML内インラインスクリプト構文チェックOK。
- `design/asset-manifest.json` はJSONとして読み込みOK。
- `http://127.0.0.1:8082/` はHTTP 200。
- 次の主要画像はHTTP 200。
  - `/assets/alagille-brand/ui-extracted/patient-avatar-harukun-v2.png`
  - `/assets/alagille-brand/ui-extracted/tile-daily-camera-v2.png`
  - `/assets/alagille-brand/trimmed/07-medicine-bottle.png`
  - `/assets/alagille-brand/trimmed/03-medical-document.png`
  - `/assets/alagille-brand/ui-extracted/wordmark-alagille-v1.png`

## 2026-06-30 追加スクリーンショット確認での指摘

森さんの実ブラウザ確認で、次の改善点を確認。

- 6つの主要アクションが2列のままだと、画像と文字が小さく、説明も読みづらい。
- `お母さん・お父さんと共有中` の家族画像に、切り出し元の変な枠が残っている。
- `成長の記録` の左画像に、植物素材の左端の余計な断片が残っている。
- 成長カードは植物よりも、子ども＋身長計の画像の方が意味が伝わりやすい。

## 2026-06-30 追加修正 2

- `.tile-grid` を2列から1列へ変更。
- `.home-action-tile` を大きい横長ボタンに変更し、画像・見出し・短い説明を読みやすくした。
- 6つの主要ボタンの説明文を短くした。
- 家族共有バナーの画像を `family-share-scene-v1.png` から `trimmed/12-family-group.png` に差し替え。
- 成長カードは `trimmed/05-growth-child-ruler.png` を使用する方針に戻した。
- 植物だけを切り直した `growth-plant-v2.png` は、補助素材として保存し `asset-manifest.json` に登録した。

## 旧実装から切り離したもの

- 既存の設定画面への直接遷移
- 既存の診察メモ画面への直接遷移
- 既存の検査値・薬・日々の様子ページへの直接遷移
- 旧ページが存在することを前提にしたホーム導線

## 未確認

- Browser Plugin / Chrome Plugin による実ブラウザスクリーンショット確認
- スマホ幅 430 x 932 での実表示
- クリック時のシート表示を実ブラウザで見た目確認
- 参照画像とのピクセル差分比較
- 追加修正後の森さん実ブラウザ目視確認

## ブロック理由

このセッションでは `mcp__node_repl__js` が `codex/sandbox-state-meta: missing field sandboxPolicy` で起動できず、Browser/Chrome正規ツールによるスクリーンショット取得ができない。

森さんとの合意により、外部Edge headless撮影は使わない。

## 現在の判定

- 実装: 完了
- 静的確認: 完了
- 視覚確認: 未完了
- 正規Browser/Chrome QA: ブロック

final result: blocked

## 2026-07-10 開発再開時の静的調整

- 家族共有の見出しを `家族と共有中` に短縮し、狭い幅での不自然な改行を抑えた。
- `最近の変化` の右側文言から日付を外し、要点だけを表示する形に短縮した。
- この変更後の視覚確認は、正規Browser/Chromeまたは森さんの実ブラウザで未実施。

## 2026-07-12 Chrome連携による再開確認

- Edge headlessとin-app Browserは使用せず、Chrome拡張連携で `http://127.0.0.1:8082/` を確認した。
- ホームのPC表示と、Chromeのviewport機能によるスマホ幅（外枠430 x 932、ページ内幅391px）を確認した。
- `家族と共有中` と短縮した最近の変化は、文字切れなし。
- 391px幅で `今日は何をしますか？` が2行へ折れたため、430px以下の専用調整を追加。修正後は1行表示、高さ約33pxを確認した。
- 患者切り替えシートを説明用シートから実動UIへ変更した。
  - `GET /api/family/patients` から、はるくん・みおちゃんを表示。
  - 選択中の患者を黄色の背景とチェックで表示。
  - みおちゃん選択後、ホーム上部の名前と年齢が切り替わることを確認。
  - 再読み込み後も選択患者が復元されることを確認。
  - お子さん追加フォームと `POST /api/family/patients` を接続。
- Chromeコンソールの警告・エラーは0件。
- 家族共有シートの本実装は次工程。

### 残るデザイン課題

- 主要6機能は説明用シートのままで、各機能画面への本遷移は未実装。

## 2026-07-12 成人対応アバター・家族共有UI

- 内蔵ImageGenで、既存のアラジール水彩素材を参照した汎用アバター4種を生成した。
  - 成人男性
  - 成人女性
  - 男の子
  - 女の子
- アバターは `backend/public/assets/alagille-brand/avatars/` へ正式保存した。
- `記録するお子さん`、`お子さんを選ぶ`、`お子さんを追加` を、成人を含む `記録する人` 表現へ変更した。
- 既存のはるくん・みおちゃんは、それぞれ男の子・女の子アバターを使用する。
- 新規追加フォームで4アバターを選択できることを、Chrome 430 x 932相当で確認した。
- 家族共有ボタンを説明シートから実動UIへ変更した。
  - `GET /api/family/members` から、お母さん・お父さんを表示。
  - 管理者、記録できる、見るだけの権限表示に対応。
  - 見るだけ／記録できるを選ぶ招待画面を追加。
  - `POST /api/family/invitations` で12文字の招待コードと共有リンクを発行。
  - Chrome上でリンクコピー後に `コピーしました` へ変わることを確認。
- Chrome撮影直後に画面上部へ黒い一時描画領域が出る場合があるが、再撮影では消え、アプリDOMには存在しない。

### この段階の未完了

- 招待リンク受け取り側の参加承認画面とLINE WORKSアカウント紐付け。
- 生成アバターの本番配信用軽量化。
- 各主要機能画面への本遷移。

## 2026-07-12 検査値画面・お試し分離・編集削除

- Edge headlessとin-app Browserは使用せず、Chrome拡張連携だけで確認した。
- 検査記録のエコー画像を、日本の採血検査結果票に近い表組みの画像へ差し替えた。
- 検査日を必須、病院名・診療科を任意として、OCR後に空欄を追記できる確認画面を実装した。受付番号欄は利用しないため削除した。
- 擬似OCRは元画像と同じ値を返す。低信頼度の誤読を主役にせず、元画像との目視確認と手修正を中心にした。
- お試し中は専用バナーを常時表示し、過去3件と今回1件を一覧・グラフへ表示した。
- お試し終了後、本番一覧が0件へ戻り、お試し値が本番保存領域へ混入しないことを確認した。
- 実記録1件を作成し、画面から病院名を編集して一覧へ反映後、削除して0件へ戻ることを確認した。
- APIの作成・一覧・推移・更新・削除、検査日必須判定、家族APIの自動テストが通過した。
- 最終画面を `C:/Users/green/Projects/.tmp/alagille-lab-final-20260712.png` に保存した。

### 検査値画面の判定

- 操作フロー: 通過
- お試しと本番記録の分離: 通過
- 編集・削除: 通過
- 架空サンプル画像の表示: 通過
- 実OCR/GCS画像保存: 未実装
- Cloud Run上の視覚確認: 未実施

## 2026-07-12 検査結果用紙v3・元画像確認UI

- 日本の外来検査結果票に近い、管理情報・検体情報・検査表・バーコードを持つ用紙へ更新。
- 6歳児に成人基準範囲を当てないよう、小児の年齢別基準範囲へ修正。
- 全8項目に当院基準範囲を表示。
- Hと高値を赤、Lと低値を青、基準範囲内を黒で表示。
- OCR結果も用紙と同じ値・基準範囲・H/Lを返す。
- 読み取り前の画像を410px高、読み取り後の元画像を430px高で表示。
- どちらもタップすると全画面拡大できる。
- 黄色の低信頼度強調を廃止し、元画像との比較案内へ変更。
- 保存前の「元の写真と見比べて、内容を確認しました」を必須化。

### 今回の検証結果

- HTML内の重複ID: 0
- 受付番号欄・旧誤読値・黄色の低信頼度表示: 実装参照なし
- OCR: 8項目、H 4件、L 1件、判定なし3件、基準範囲欠損0
- v3画像: HTTP 200
- 検査値API・家族APIテスト: 通過
- ローカルサーバーのエラーログ: 空
- Chrome連携: 同じ検査値タブの状態取得が2回連続タイムアウトしたため停止。今回の最終視覚確認は未実施
